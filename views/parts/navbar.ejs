<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                    <% if (isUser) { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/groups">Groups</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/profile">profile</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/friends">Friends</a>
                        </li> 
                    <% } else { %> 
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/signup">Signup</a>
                    </li>
                    <% } %> 
                </ul> 
                <% if (isUser) { %>
                    <input type="hidden" id="myId" value="<%= isUser %>">
                    <div class="dropdown">
                        <button class="btn btn-primary dropdown-toggle" type="button" id="friendRequestsDropdown" data-bs-toggle="dropdown" aria-expanded="true" >
                            friend requests
                        </button>
                        <ul id="friendRequests" class="dropdown-menu" aria-labelledby="friendRequestsDropdown">
                            <li >
                                <% if (friendRequests.length === 0 ) { %>
                                    <span id="span">  no friend requests</span>
                                <% }  %>
                                    <% for(let request of friendRequests) { %>
                                        <a class="dropdown-item" href="/profile/<%= request.id %>">
                                            <%= request.name %>
                                        </a>   
                                    <% } %>
                                
                            </li>
                        </ul>
                    </div>
                    <form action="/logout" method="POST">
                        <input type="submit" value="LOGOUT" class="btn btn-danger">
                    </form>
                <% } %>  
            </div> 
    </div>
</nav>




